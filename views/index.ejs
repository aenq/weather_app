<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" media="screen" href="/css/style.css">
    <title>Weather App</title>
</head>
<body>

    <div class="container">
        <h1> Check current world weather information. </h1>
        <fieldset>
            <form action="/" method="post">
                <input type="text" class="ghost-input" name="city" id="city" placeholder="Enter a City" required> <br>
                <input type="submit" class="ghost-button" value="Get Weather"> 
            </form>
            <% if ( weather !== null) { %>
                <p><%= weather %></p> 
            <% } %>

            <% if (error !== null) { %>
                <p><%= error %></p> 
            <% } %>

        </fieldset>
    </div>
    
</body>
</html>